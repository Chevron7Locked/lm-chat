apiVersion: v1
kind: Service
metadata:
  name: lmchat
  labels:
    app: lmchat
spec:
  type: ClusterIP
  selector:
    app: lmchat
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
