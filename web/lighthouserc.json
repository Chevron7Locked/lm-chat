{
  "_comment": "TODO: /chats/<id> and /project/<id> require an authenticated session. LHCI's staticDistDir has no auth mechanism, so these routes will resolve via isSinglePageApplication but the SPA will redirect to login. Real measurement requires a puppeteerScript that seeds a session cookie (e.g. from a test user fixture) before collect.",
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "isSinglePageApplication": true,
      "url": [
        "http://localhost/",
        "http://localhost/login",
        "http://localhost/chats/42",
        "http://localhost/project/42"
      ]
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.9 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }]
      }
    }
  }
}